info:
  name: List account
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/accounts?page_number=0&page_size=10"
  params:
    - name: page_number
      value: "0"
      type: query
    - name: page_size
      value: "10"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
