info:
  name: Delete Account By ID
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/accounts/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
