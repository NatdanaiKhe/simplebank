info:
  name: Get Account By ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/accounts/{{id}}"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: id
      value: "3"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
