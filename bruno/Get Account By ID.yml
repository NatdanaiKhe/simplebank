info:
  name: Get Account By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/accounts/20"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
