version: "2"
sql:
  - name: "bank"
    engine: "postgresql"
    queries: "db/query"
    schema: "db/schema"

    gen:
      go:
        package: "db"
        out: "db/sqlc"
        emit_empty_slices: true
        emit_interface: true
